{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "scripts/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-watch": "sass --watch ."
  },
  "private": true,
  "devDependencies": {
    "sass": "^1.98.0"
  }
}
